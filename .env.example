GARMIN_USERNAME=your-garmin-email@example.com
GARMIN_PASSWORD=your-garmin-password

# HTTP server mode (set PORT to enable HTTP mode instead of stdio)
PORT=3000

# API key for MCP client authentication (optional but recommended)
API_KEY=your-secret-api-key
